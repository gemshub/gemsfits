# search maching expdatasets and samples; with sT and sP in a certain interval
# leave out fields that are not necessary in the search e.g. "sT" :   {'$bt' : [num1, num2]}
# '$in' - matches the values
# '$bt' - between two values

{
     "expdataset" :   {'$in' : ["val1", "val2", "val3"]},
     "sample" :   {'$in' : ["val1", "val2", "val3"]},
     "sT" :   {'$bt' : [num1, num2]},
     "sP" :   {'$bt' : [num1, num2]}
}
